# Archive

Historical implementation plans and design notes. Paths and product names here (e.g. ParakeetPTT) are outdated.

Current source of truth: root `README.md`, `CLAUDE.md`, and `swift/` / `src/`.
